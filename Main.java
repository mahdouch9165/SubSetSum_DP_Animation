import SubSetSum.SubSetSum;
public class Main {
    //Main method
    public static void main(String[] args) {
        SubSetSum subsetSum = new SubSetSum();
        //Uncomment the following line to run the algorithm animation in auto mode
        subsetSum.setPauseType("auto");
        subsetSum.setPause(0);
        subsetSum.start();
    }
}